# KanbanBoard

## Task
Доска Канбан (аналог Trello, упрощенный):
Бэкенд: Ticket (название, колонка — TODO, IN_PROGRESS, DONE).
Angular: Отобразить три колонки. Реализовать (можно без Drag&Drop, просто кнопками "Вправо/Влево") перемещение карточек между колонками с отправкой PUT-запроса на смену статуса.

## To run
docker compose up -d --build

## UI
Navigate to `http://localhost:4200/`.